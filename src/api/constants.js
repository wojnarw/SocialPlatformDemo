const URL = "http://localhost:8080/api/";
const KEY = "api_key";

//endpoints:
const API = {
    POSTS: URL + "posts",
    TOP: URL + "top",
    NEW: URL + "post/new",
    POST: URL + "post/"
}

export default API;